apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: medical-qa
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 2

  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/dongbin-lee-git/agent_test-backend:latest
          ports:
            - containerPort: 8001
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"

          readinessProbe:
            httpGet:
              path: /agent/health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 5
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret

---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: medical-qa
spec:
  selector:
    app: backend
  ports:
    - port: 8001
      targetPort: 8001